# Kubernetes namespace for the whole stack.
# Apply everything at once:  kubectl apply -k infrastructure/kubernetes
apiVersion: v1
kind: Namespace
metadata:
  name: ticket-booking
  labels:
    app.kubernetes.io/part-of: ticket-booking
